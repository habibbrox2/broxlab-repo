<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
 {% for map in sitemaps %}
 <sitemap>
 <loc>{{ (base_url()|trim('/', 'right')) ~ map.loc }}</loc>
 <lastmod>{{ map.lastmod|date('Y-m-d') }}</lastmod>
 </sitemap>
 {% endfor %}
</sitemapindex>

