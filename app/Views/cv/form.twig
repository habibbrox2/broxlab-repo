{% extends 'layout.twig' %}

{% block title %}{{ page_title|default('Build Your CV') }}{% endblock %}

{% block extra_styles %}
<style>
/* ═══════════════════════════════════════════════════════
   CV BUILDER — Tailwind-powered · Minimal custom CSS
   Only JS-dynamic classes (generated by cv-builder.js)
   ═══════════════════════════════════════════════════════ */

/* ── JS-dynamic form elements (cv-builder.js injects these class names) ── */
.bld-form-group { margin-bottom: 1.5rem; }
.bld-input-group { display: grid; grid-template-columns: 1fr; gap: 1rem; }
@media (min-width: 480px) { .bld-input-group { grid-template-columns: 1fr 1fr; } }
.bld-input-group-3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 1rem; }
.bld-label { display: block; font-weight: 600; color: #374151; margin-bottom: 0.4rem; font-size: 0.875rem; }

.bld-input,
.bld-select,
.bld-textarea {
  width: 100%; padding: 0.75rem 1rem;
  border: 2px solid #e5e7eb; border-radius: 10px;
  font-size: 0.95rem; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  background: #f9fafb; font-family: inherit; box-sizing: border-box; color: #1f2937;
}
.bld-input::placeholder, .bld-textarea::placeholder { color: #9ca3af; }
.bld-input:hover, .bld-select:hover, .bld-textarea:hover { border-color: #d1d5db; background: white; }
.bld-input:focus, .bld-select:focus, .bld-textarea:focus {
  outline: none; border-color: #6366f1; background: white;
  box-shadow: 0 0 0 4px rgba(99,102,241,0.1);
}
.bld-textarea { min-height: 120px; resize: vertical; line-height: 1.6; }

/* ── Suggestion boxes ── */
.bld-suggest-box {
  background: linear-gradient(135deg, #eef2ff, #f5f3ff);
  border: 1px solid #c7d2fe; border-radius: 10px;
  padding: 1.25rem; margin-bottom: 1rem; position: relative; overflow: hidden;
}
.bld-suggest-box::before {
  content: ''; position: absolute; top: 0; left: 0;
  width: 4px; height: 100%;
  background: linear-gradient(180deg, #6366f1, #8b5cf6);
  border-radius: 0 2px 2px 0;
}
.bld-suggest-box h4 { font-size: 0.85rem; font-weight: 600; color: #4f46e5; margin-bottom: 0.5rem; display: flex; align-items: center; gap: 0.375rem; }
.bld-suggest-text { font-size: 0.9rem; color: #374151; line-height: 1.6; margin-bottom: 0.75rem; padding-left: 0.5rem; }
.bld-suggest-actions { display: flex; gap: 0.5rem; flex-wrap: wrap; }

.bld-btn-sm {
  padding: 0.4rem 0.875rem; border-radius: 8px; font-size: 0.8rem;
  font-weight: 600; border: none; cursor: pointer;
  transition: all 0.2s ease;
  display: inline-flex; align-items: center; gap: 0.3rem;
}
.bld-btn-accept { background: #10b981; color: white; }
.bld-btn-accept:hover { background: #059669; transform: translateY(-1px); box-shadow: 0 4px 12px rgba(16,185,129,0.3); }
.bld-btn-edit { background: #f59e0b; color: white; }
.bld-btn-edit:hover { background: #d97706; transform: translateY(-1px); }
.bld-btn-remove { background: #ef4444; color: white; }
.bld-btn-remove:hover { background: #dc2626; transform: translateY(-1px); }

/* ── Entry cards (Experience, Education, etc.) ── */
.bld-entry-card {
  background: white; border: 2px solid #e5e7eb; border-radius: 10px;
  padding: 1.5rem; margin-bottom: 1rem; position: relative;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
.bld-entry-card:hover { border-color: #d1d5db; box-shadow: 0 4px 16px rgba(0,0,0,0.06); }
.bld-entry-card .bld-entry-remove {
  position: absolute; top: 0.75rem; right: 0.75rem;
  background: #f3f4f6; border: none; color: #9ca3af;
  cursor: pointer; width: 28px; height: 28px; border-radius: 8px;
  display: flex; align-items: center; justify-content: center;
  transition: all 0.2s ease;
}
.bld-entry-card .bld-entry-remove:hover { background: #fef2f2; color: #ef4444; transform: scale(1.1); }
.bld-entry-card .bld-input-group { gap: 0.75rem; }
.bld-entry-card .bld-form-group { margin-bottom: 0.75rem; }

.bld-add-entry {
  display: inline-flex; align-items: center; justify-content: center; gap: 0.5rem;
  color: #6366f1; font-weight: 600; font-size: 0.9rem; cursor: pointer;
  padding: 0.75rem 1.25rem; border: 2px dashed #6366f1; border-radius: 10px;
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  background: transparent; width: 100%; box-sizing: border-box;
}
.bld-add-entry:hover { background: #eef2ff; border-color: #4f46e5; transform: translateY(-1px); }

/* ── Skills ── */
.bld-skills-area { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-top: 0.5rem; min-height: 2.5rem; }
.bld-skill-tag {
  display: inline-flex; align-items: center; gap: 0.3rem;
  padding: 0.4rem 0.875rem; background: #eef2ff; color: #4f46e5;
  border: 1px solid #c7d2fe; border-radius: 20px;
  font-size: 0.85rem; font-weight: 500;
  transition: all 0.2s ease; animation: tagPop 0.25s ease-out;
}
@keyframes tagPop { from { transform: scale(0.8); opacity: 0; } to { transform: scale(1); opacity: 1; } }
.bld-skill-tag:hover { background: #ddd6fe; border-color: #a78bfa; }
.bld-skill-tag .bld-skill-remove {
  cursor: pointer; width: 18px; height: 18px; border-radius: 50%;
  display: inline-flex; align-items: center; justify-content: center;
  color: #9ca3af; background: none; border: none; padding: 0;
  transition: all 0.2s ease;
}
.bld-skill-tag .bld-skill-remove:hover { background: rgba(239,68,68,0.15); color: #ef4444; }
.bld-skill-input {
  border: 2px dashed #d1d5db; border-radius: 20px; padding: 0.4rem 0.875rem;
  font-size: 0.85rem; background: transparent; min-width: 140px;
  box-sizing: border-box; transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}
.bld-skill-input:focus { outline: none; border-color: #6366f1; border-style: solid; background: white; box-shadow: 0 0 0 3px rgba(99,102,241,0.1); }

/* ── Checkbox ── */
.bld-checkbox {
  display: inline-flex; align-items: center; gap: 0.5rem;
  cursor: pointer; font-size: 0.9rem; color: #4b5563;
  padding: 0.5rem 0; user-select: none;
}
.bld-checkbox input[type="checkbox"] { width: 18px; height: 18px; accent-color: #6366f1; border-radius: 4px; }

/* ── Toast / Notification ── */
.bld-toast {
  position: fixed; bottom: 1.5rem; left: 50%;
  transform: translateX(-50%) translateY(80px);
  background: #111827; color: white; padding: 0.75rem 1.5rem;
  border-radius: 10px; font-size: 0.9rem; opacity: 0;
  transition: all 0.3s ease; z-index: 100;
  box-shadow: 0 8px 32px rgba(0,0,0,0.3);
  display: flex; align-items: center; gap: 0.5rem; pointer-events: none;
}
.bld-toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }

/* ── Step dot states (toggled by cv-builder.js) ── */
.bld-step-dot.active {
  background: white !important;
  border-color: white !important;
  color: #4f46e5 !important;
  box-shadow: 0 0 16px rgba(255,255,255,0.5);
  transform: scale(1.15);
}
.bld-step-dot.completed {
  background: #10b981 !important;
  border-color: #10b981 !important;
  color: transparent !important;
  position: relative;
}
.bld-step-dot.completed::after {
  content: '✓';
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.8rem;
  color: white;
}

/* ── Auto-save indicator show state ── */
.bld-save-indicator.show {
  opacity: 1 !important;
  transform: translateY(0) !important;
}

/* ── Template card selected state (toggled by cv-builder.js) ── */
.bld-template-card.selected {
  border-color: #6366f1 !important;
  background: #eef2ff !important;
  box-shadow: 0 0 0 4px rgba(99,102,241,0.12), 0 8px 24px rgba(99,102,241,0.12) !important;
}
.bld-template-card.selected::after {
  content: '✓';
  position: absolute;
  top: 0.5rem;
  right: 0.5rem;
  width: 22px;
  height: 22px;
  border-radius: 50%;
  background: #6366f1;
  color: white;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.75rem;
  font-weight: 700;
}

/* ── Step card accent bar ── */
.bld-card-accent::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0;
  height: 4px;
  background: linear-gradient(90deg, #6366f1, #8b5cf6, #14b8a6);
  opacity: 0.6;
}

/* ── Progress fill glow ── */
@keyframes progressGlow {
  0%, 100% { opacity: 0.4; }
  50% { opacity: 1; }
}

/* ── Step content animation ── */
@keyframes stepFadeIn {
  from { opacity: 0; transform: translateY(12px); }
  to { opacity: 1; transform: translateY(0); }
}
#bld-step-content { animation: stepFadeIn 0.35s ease-out; }

/* ── Loading shimmer for disabled buttons ── */
@keyframes bldShimmer {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(100%); }
}

/* ── Step indicators: scroll snap on mobile ── */
@media (max-width: 480px) {
  .bld-step-indicators-scroll {
    overflow-x: auto; -webkit-overflow-scrolling: touch;
    scroll-snap-type: x mandatory;
    padding-bottom: 0.375rem;
    scrollbar-width: none; -ms-overflow-style: none;
  }
  .bld-step-indicators-scroll::-webkit-scrollbar { display: none; }
  .bld-step-indicators-scroll > * { scroll-snap-align: center; flex-shrink: 0; }
}

/* ── Reduced motion ── */
@media (prefers-reduced-motion: reduce) {
  .bld-progress-fill, .bld-progress-fill::after,
  #bld-step-content, .bld-step-dot, .bld-skill-tag {
    animation: none !important; transition: none !important;
  }
}

/* ── Touch device optimizations ── */
@media (hover: none) and (pointer: coarse) {
  .bld-input, .bld-select, .bld-textarea { font-size: 16px !important; }
  .bld-btn, .bld-add-entry, .bld-template-card { min-height: 44px; }
}
</style>
{% endblock %}

{% block content %}
<div class="min-h-screen bg-gradient-to-br from-[#667eea] to-[#764ba2] p-6 md:p-8 relative overflow-hidden" id="bld-app">
  <!-- Decorative gradient orbs -->
  <div class="absolute inset-0 pointer-events-none"
       style="background: radial-gradient(ellipse at 20% 50%, rgba(255,255,255,0.08) 0%, transparent 50%),
                     radial-gradient(ellipse at 80% 20%, rgba(255,255,255,0.05) 0%, transparent 50%),
                     radial-gradient(ellipse at 50% 80%, rgba(255,255,255,0.06) 0%, transparent 50%);">
  </div>

  <div class="max-w-4xl mx-auto relative z-10">

    <!-- Job Position Selector -->
    <div class="mb-6 sticky top-3 z-20">
      <select id="bld-job-title" onchange="window.bldSelectJob(this.value)"
              class="w-full px-5 py-3.5 border-2 border-white/25 rounded-xl bg-white/15 backdrop-blur-md text-white text-base font-medium cursor-pointer appearance-none transition-all duration-300 hover:bg-white/20 hover:border-white/40 focus:outline-none focus:border-white/60 focus:bg-white/25 focus:ring-4 focus:ring-white/10"
              style="background-image: url('data:image/svg+xml,%3Csvg xmlns=%27http://www.w3.org/2000/svg%27 width=%2720%27 height=%2720%27 viewBox=%270 0 24 24%27 fill=%27none%27 stroke=%27white%27 stroke-width=%272%27 stroke-linecap=%27round%27 stroke-linejoin=%27round%27%3E%3Cpolyline points=%276 9 12 15 18 9%27%3E%3C/polyline%3E%3C/svg%3E');
                     background-repeat: no-repeat; background-position: right 1rem center; background-size: 1.25rem; padding-right: 3rem;">
        <option value="" class="text-gray-800 bg-white">🎯 Select your job title for personalized suggestions...</option>
        {% for pos in job_positions %}
        <option value="{{ pos.slug }}" class="text-gray-800 bg-white">{{ pos.title }}</option>
        {% endfor %}
      </select>
    </div>

    <!-- Progress Bar -->
    <div class="bg-white/10 backdrop-blur-xl rounded-2xl p-6 mb-6 border border-white/15 shadow-lg">
      <div class="flex justify-between items-center mb-3.5">
        <span class="text-white font-semibold flex items-center gap-2">
          <span class="inline-flex items-center justify-center w-7 h-7 rounded-full bg-white/20 text-[0.8rem] font-bold" id="bld-current-step">1</span>
          <span>Step 1 of 12</span>
        </span>
        <span class="text-white/85 text-sm font-medium bg-white/10 px-3 py-1 rounded-full">
          <span id="bld-progress-pct">0</span>% Complete
        </span>
      </div>
      <div class="h-1.5 bg-white/12 rounded-full overflow-hidden mb-4 relative">
        <div class="h-full bg-gradient-to-r from-indigo-300 to-white rounded-full transition-all duration-[0.6s] ease-out relative bld-progress-fill" id="bld-progress-fill" style="width: 0%;"></div>
      </div>
      <div class="flex justify-between relative py-1 bld-step-indicators-scroll" id="bld-step-dots">
        <div class="absolute top-1/2 left-0 right-0 h-0.5 bg-white/10 -translate-y-1/2 pointer-events-none"></div>
        {# Step indicators #}
        {% set steps = [
          {'label': 'Personal', 'num': '1'}, {'label': 'Summary', 'num': '2'}, {'label': 'Experience', 'num': '3'},
          {'label': 'Education', 'num': '4'}, {'label': 'Skills', 'num': '5'}, {'label': 'Languages', 'num': '6'},
          {'label': 'Certifications', 'num': '7'}, {'label': 'Projects', 'num': '8'}, {'label': 'Social', 'num': '9'},
          {'label': 'Custom', 'num': '10'}, {'label': 'References', 'num': '11'}, {'label': 'Review', 'num': '12'}
        ] %}
        {% for step in steps %}
        <div class="flex flex-col items-center gap-1.5 relative z-10 cursor-pointer transition-transform duration-300 hover:-translate-y-0.5 min-h-[44px] md:min-h-0 justify-center" title="{{ step.label }}">
          <div class="bld-step-dot w-8 h-8 md:w-8 md:h-8 rounded-full bg-white/15 border-2 border-white/25 transition-all duration-[0.4s] ease-out flex items-center justify-center text-xs font-bold text-white/50 relative {{ step.num == '1' ? 'bg-white border-white text-indigo-700 scale-110 shadow-[0_0_16px_rgba(255,255,255,0.5)]' : '' }}"
               data-step="{{ step.num }}"
               style="{% if step.num == '1' %}color: #4f46e5;{% endif %}">
            <span {{ step.num }}</span>
          </div>
          <span class="text-[0.6rem] text-white/60 whitespace-nowrap font-medium tracking-wide hidden sm:block">{{ step.label }}</span>
        </div>
        {% endfor %}
      </div>
    </div>

    <!-- Step Content -->
    <div class="bg-white rounded-2xl p-6 md:p-10 shadow-2xl border border-white/20 relative overflow-hidden bld-card-accent" id="bld-step-content">
      <!-- JavaScript populates this -->
    </div>

    <!-- Template Selector (shown before finish) -->
    <div id="bld-template-selector" style="display:none;" class="bg-white rounded-2xl p-6 md:p-10 shadow-2xl border border-white/20 relative overflow-hidden bld-card-accent mt-6">
      <div class="mb-8 pb-6 border-b-2 border-gray-100">
        <div class="inline-flex items-center gap-2 text-sm text-indigo-600 font-semibold mb-2 uppercase tracking-wide">
          <i class="lucide lucide-palette w-4 h-4"></i> Final Step
        </div>
        <h2 class="text-2xl md:text-3xl font-bold text-gray-800 mb-1.5 leading-tight">Choose Your Template</h2>
        <p class="text-gray-500">Select a template style that best presents your professional profile.</p>
      </div>
      <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4">
        {% for tmpl in templates %}
        <div class="bld-template-card border-2 border-gray-200 rounded-xl p-5 text-center cursor-pointer transition-all duration-300 relative hover:border-gray-300 hover:bg-gray-50 hover:-translate-y-0.5 hover:shadow-lg {{ tmpl == selected_template ? 'border-indigo-500 bg-indigo-50 shadow-[0_0_0_4px_rgba(99,102,241,0.12)] selected' : '' }}"
             data-template="{{ tmpl }}" onclick="window.bldSelectTemplate('{{ tmpl }}')">
          <div class="w-12 h-12 rounded-xl flex items-center justify-center mx-auto mb-2.5 text-white text-xl transition-transform duration-300 group-hover:scale-110"
               style="background: linear-gradient(135deg, {{ tmpl == 'modern' ? '#6366f1,#8b5cf6' : tmpl == 'minimal' ? '#374151,#6b7280' : tmpl == 'ats' ? '#059669,#10b981' : '#1e40af,#3b82f6' }});">
            <i class="lucide lucide-{{ tmpl == 'modern' ? 'palette' : tmpl == 'minimal' ? 'minus' : tmpl == 'ats' ? 'bot' : 'briefcase' }} w-5 h-5"></i>
          </div>
          <div class="font-semibold text-gray-800">{{ tmpl|capitalize }}</div>
        </div>
        {% endfor %}
      </div>
      <div class="mt-4 text-center">
        <a href="/cv-builder/templates"
           class="inline-flex items-center gap-2 text-indigo-600 text-sm font-semibold no-underline px-4 py-2 rounded-lg transition-all duration-200 hover:bg-indigo-50">
          <i class="lucide lucide-external-link w-4 h-4"></i>
          Browse Full Template Marketplace
        </a>
      </div>
    </div>

    <!-- Navigation -->
    <div class="flex justify-between items-center mt-8 pt-6 border-t-2 border-white/15 gap-3 flex-wrap">
      <button class="flex items-center gap-2 px-6 py-3 rounded-full font-semibold text-sm border-none cursor-pointer transition-all duration-200 active:scale-[0.97] bg-white/15 text-white/80 hover:bg-white/25 hover:text-white" id="bld-btn-prev" onclick="window.bldPrevStep()">
        <i class="lucide lucide-chevron-left w-4 h-4"></i> Back
      </button>
      <div class="flex gap-3">
        <button class="flex items-center gap-2 px-6 py-3 rounded-full font-semibold text-sm border-2 border-white/20 cursor-pointer transition-all duration-200 active:scale-[0.97] bg-white text-gray-600 hover:bg-gray-50 hover:border-gray-300 hover:-translate-y-0.5" id="bld-btn-save-draft" onclick="window.bldSaveDraft()">
          <i class="lucide lucide-save w-4 h-4"></i> Save
        </button>
        <button class="flex items-center gap-2 px-6 py-3 rounded-full font-semibold text-sm border-none cursor-pointer transition-all duration-200 active:scale-[0.97] bg-gradient-to-br from-indigo-500 to-purple-600 text-white shadow-[0_4px_16px_rgba(99,102,241,0.35)] hover:-translate-y-0.5 hover:shadow-[0_8px_28px_rgba(99,102,241,0.45)] disabled:opacity-50 disabled:cursor-not-allowed disabled:!translate-y-0 disabled:!shadow-none" id="bld-btn-next" onclick="window.bldNextStep()">
          Next <i class="lucide lucide-chevron-right w-4 h-4"></i>
        </button>
      </div>
    </div>

    <!-- Auto-save Indicator -->
    <div class="fixed bottom-6 right-6 bg-gray-900/90 backdrop-blur-sm text-white px-4 py-2.5 rounded-xl text-sm opacity-0 translate-y-2.5 transition-all duration-300 z-[100] flex items-center gap-2 shadow-2xl border border-white/10 bld-save-indicator" id="bld-save-indicator">
      <i class="lucide lucide-check-circle w-4 h-4 text-emerald-500"></i>
      Auto-saved
    </div>
  </div>
</div>

<script src="/assets/js/dist/cv-builder.js?v={{ random() }}" defer></script>
<script>
window.__bldData = {{ builder_data|json_encode|raw }};
window.__bldCvId = {{ cv_id|raw }};
window.__bldCsrf = '{{ csrf_token|default('') }}';
window.__bldJobPositions = {{ job_positions|json_encode|raw }};
window.__bldTemplates = {{ templates|json_encode|raw }};
window.__bldSelectedTemplate = '{{ selected_template }}';
</script>
{% endblock %}
