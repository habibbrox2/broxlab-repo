{# 2026 Standard AI Copilot Sidebar for Admin Panel #}
<div id="adminAiShell" class="ai-chat-shell admin-sidebar hidden">
    <div class="ai-header admin">
        <div class="ai-header-info">
            <div class="ai-header-title" id="adminAiTitle">Admin Copilot</div>
            <div class="ai-header-status" id="adminAiStatus">Context: Dashboard</div>
        </div>
        <div class="ai-header-actions">
            <button id="adminAiClose" class="btn-close btn-close-white" style="font-size: 0.8rem;"></button>
        </div>
    </div>

    {# Chat History Body #}
    <div id="adminAiBody" class="ai-body">
        {# Messages & Artifacts injected via ai-admin.js #}
    </div>

    {# Slash Menu (Hidden by default) #}
    <div id="adminAiSlashMenu" class="ai-slash-menu hidden">
        <div class="ai-slash-item" data-cmd="/generate">
            <span class="cmd">/generate</span>
            <span class="desc">Create new content or data</span>
        </div>
        <div class="ai-slash-item" data-cmd="/summarize">
            <span class="cmd">/summarize</span>
            <span class="desc">Summarize current page data</span>
        </div>
        <div class="ai-slash-item" data-cmd="/analyze">
            <span class="cmd">/analyze</span>
            <span class="desc">Deep analyze patterns</span>
        </div>
    </div>

    {# Chat Input Footer #}
    <div id="adminAiFooter" class="ai-footer">
        <div class="ai-composer">
            <input type="text" id="adminAiInput" placeholder="Type / for commands..." autocomplete="off">
            <button id="adminAiSend" class="ai-send-btn bg-dark">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M22 2L11 13M22 2l-7 20-4-9-9-4 20-7z"/>
                </svg>
            </button>
        </div>
    </div>
</div>

{# Admin Sidebar Toggle (Minimalist) #}
<button id="adminAiBtn" class="admin-assistant-fab">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
    </svg>
</button>

<link rel="stylesheet" href="{{ asset('/ai/css/ai-style.css') }}">
<script src="{{ asset('/ai/js/ai-admin.js') }}"></script>
