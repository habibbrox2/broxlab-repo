Read: `AGENTS.md`, then `docs/ai/AI_QUICK_CONTEXT.md`.

Do:
- Search first (`rg`), then make minimal diffs.
- Keep controllers thin; DB access in Models (prepared statements).
- Enforce CSRF for state-changing endpoints.

Do not:
- Edit `public_html/assets/**/dist/**` directly.
- Commit secrets or paste real tokens/keys.

Verify:
- `php -l path/to/file.php`
- `php scripts/quality_scan.php`

